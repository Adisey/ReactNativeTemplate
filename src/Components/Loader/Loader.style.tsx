import { StyleSheet } from 'react-native';

export const LoaderStyles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    display: 'flex',
    alignItems: 'center',
  },
  text: {
    fontSize: 20,
  },
});
