import { StyleSheet } from 'react-native';

export const LogoStyles = StyleSheet.create({
  container: {
    alignItems: 'center',
    display: 'flex',
    height: 200,
    justifyContent: 'center',
    marginTop: 20,
    width: '100%',
  },
  img: {
    height: '100%',
    width: 200,
  },
});
